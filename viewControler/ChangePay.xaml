<Page x:Class="BankManagement_Assignment.viewControler.ChangePay"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BankManagement_Assignment.viewControler"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChangePay">

    <Grid>
        <DataGrid x:Name="datagrid2" HorizontalAlignment="Left" Height="326" VerticalAlignment="Top" Width="800" >
            <DataGrid.Columns>
                <!--<DataGridTextColumn Header="员工号" Binding="{Binding Employer_ID}" />
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" />
                <DataGridTextColumn Header="性别" Binding="{Binding Gender}" />
                <DataGridTextColumn Header="年龄" Binding="{Binding Age}" />
                <DataGridTextColumn Header="工资" Binding="{Binding Sallery}" />-->

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
